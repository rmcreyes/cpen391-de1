#include <stdlib.h>
#include <stdint.h>
#include <stdio.h>

int init_accel(int* input, int in_size, int l1_size, int l2_size, int l3_size)
{
    printf("%d ", in_size);
    printf("%d ", l1_size);
    printf("%d ", l2_size);
    printf("%d ", l3_size);
}